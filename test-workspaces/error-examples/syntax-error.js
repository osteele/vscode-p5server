// This sketch has a syntax error for testing console output
function setup() {
  createCanvas(400, 400);
  console.log('Starting sketch with error');
  
  // Syntax error: missing closing parenthesis
  fill(255, 0, 0;
}

function draw() {
  background(220);
  circle(200, 200, 100);
}