// Project A main sketch
function setup() {
  createCanvas(300, 300);
  console.log('Project A initialized');
}

function draw() {
  background(100, 150, 200);
  fill(255);
  textAlign(CENTER, CENTER);
  text('Project A', width / 2, height / 2);
}